{
  "name": "password-generator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
